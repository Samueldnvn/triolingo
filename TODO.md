# TODO.md To-do list

- (COMPLETED) Build a desktop app that is a UI with a screen viewer that displays the screen. Next to the screen is a chat channel. The app will take screenshots of the screen and enter it into an Ollama vision model at a given rate, and the vision model will determine what to do on the screen. The app should have a second mouse and second keyboard that interfaces with the Ollama vision model so that it can control the keyboard and mouse separate from the one that the user has. The chat channel is so that the user can interact with the vision model as it is performing tasks to give it orders. The overall objective of this is to create an AI agent that is controls the computer through a vision model taking in the screen. The UI should allow you to set the rate in which the app is screenshotting and feeding to the vision model. And the app should have a clear easy way to turn it off. Name the app "White Mirror".

- (COMPLETED) Use the "Course Builder" skill to create a course that teaches master millitary strategy and manoeuvering up until the Napoleonic era. It should cover everything one needs to know about every single strategy for moving and attacking, defending, etc, that were used to win battles. The style of the site should be a very distinct Rennaisance style, as though it were heavily styled to be a site about Da Vinci.

- (COMPLETED) Use the "Course Builder" skill to create a complete course on Minecraft redstone computer building and logic. It should teach the user everything they need to know to build a Minecraft redstone computer from scratch. Assume the user already knows all about how redstone works, just that they don't understand the building logic gates all the way to computer architecture. Make sure it teaches even more than they need to know. The style of the site should be heavily Minecraft themed and heavily styled.

- (COMPLETED) Use the "Course Builder" skill to create a plain course that will be able to be changed to have a style later. But now make all of the course content and site, which should be about competitive programming using Python, from beginner all the way to expert. I want it to start with Data Structures and Algorithms, and then go over literally every single thing needed to be known to solve all the Leetcode problems (for example, sliding window). It should teach the user when and how to implement everything. It should be very extensive and eventually build up to very advanced algorithms and lesser known ones. Assume that the user already knows all the basics of Python. 

- Use the "Course Builder" skill to create a plain course that will be able to be changed to have a style later. But now make all of the course content and site, which should be about IoT, and how the user can program and develop it. It should go into depth about tools code, using Java. 

- Use the "Course Builder" skill to create a plain course that will be able to be changed to have a style later. But now make all of the course content and site, which should be about how the user can develop a game using Unity and mostly C++ rather than C#, through DLLs. 

- Use the "Course Builder" skill to create a plain course that will be able to be changed to have a style later. But now make all of the course content and site, which should be about machine learning in Python. It should cover everything, beginner to advanced in machine learning. By the end of the course the user should be able to develop transformers. 

- Use the "Course Builder" skill to create a plain course that will be able to be changed to have a style later. But now make all of the course content and site, which should be about investing in Covered Call Options and making a lot of money off them. The course should teach the user everything technical and fundamental about them.